# Dependencies
node_modules/

# Build outputs
site/.astro/
site/dist/
site/node_modules/

# Coverage
coverage/
.nyc_output/

# Tooling caches
.polyglot-cache.json
.npm/
.DS_Store
Thumbs.db

# Local archives (the release zip is produced fresh at tag-time)
sovereign-v*-release.zip
*.tgz

# Editor
.vscode/
.idea/
*.swp
*~

# Logs
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# Regenerable simulation raw data — reproducible byte-identical from seeds
# (determinism thesis). The analysis scripts + reports are tracked; the bulk
# JSON outputs are not.
experiments/**/raw-data/
tools/**/raw-data/

# Dogfood-swarm working artifacts (manifest, findings, receipts) — local only
swarms/
